import { Flex } from '@mantine/core';
import type { FC } from 'react';

export interface ErrorDetailProps {}

export const ErrorDetail: FC<ErrorDetailProps> = (_props) => {
  return (
    <Flex mih={50} h={''} justify="center" align="center" wrap="wrap">
      div
    </Flex>
  );
};
